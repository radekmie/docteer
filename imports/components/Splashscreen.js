// @flow
// @jsx h

import {h} from 'preact';

export function Splashscreen () {
  return (
    <div class="flex-1 pt3 tc w10 w-100">
      There's no description nor introduction yet, but there's an demo account: <code>demo@docteer.com:demo</code>. It has access to all features, but keep in mind, that changing its password won't do anything.
    </div>
  );
}
